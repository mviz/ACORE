Matthew Fritze


You'll need python2.7 and django version 1.6.5
Install with pip

$pip install Django==1.6.5

For all the required packages run:
#pip install -r requirements.txt

To see the site, from the root directory run the command:
$python manage.py runserver

In a browser go to:
127.0.0.1:8000/